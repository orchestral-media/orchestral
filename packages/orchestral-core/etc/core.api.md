## API Report File for "@orchestral/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { z } from 'zod';
import { ZodRawShape } from 'zod';
import { ZodType } from 'zod';

// @public
export interface AddFromManifestOptions {
    readonly missingOps?: 'throw' | 'skip';
    readonly only?: readonly string[];
}

// @public
export interface AddFromManifestResult {
    readonly registered: readonly PatternId[];
    readonly skipped: readonly SkippedManifestPattern[];
}

// @public
export const AGENT_BASE_INPUT_SCHEMA: z.ZodObject<{
    description: z.ZodString;
    prompt: z.ZodString;
    references: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnion<readonly [z.ZodString, z.ZodArray<z.ZodString>]>>>;
}, z.core.$strip>;

// @public
export const AGENT_FINISH_TOOL_NAME = "complete_task";

// @public
export type AgentBaseInput = z.infer<typeof AGENT_BASE_INPUT_SCHEMA>;

// @alpha
export interface AgentDispatchEnvelope {
    errorMessage?: string;
    patternId: string;
    runId?: string;
    status: 'completed' | 'errored';
    text: string;
    totalDurationMs: number;
    totalToolUseCount: number;
    transcriptId?: string;
    usage?: {
        totalTokens?: number;
    };
}

// @public
export interface AgentFinishSpec<TFinish = unknown, TOut = unknown> {
    // (undocumented)
    compose: (finish: TFinish, facts: AgentRunFacts) => TOut;
    // (undocumented)
    inputs: ZodSchema<TFinish>;
}

// @public
export function agentInputSchema<TExtra extends z.ZodRawShape>(extra: TExtra): z.ZodObject<(("description" | "references" | "prompt") & keyof TExtra extends never ? {
    description: z.ZodString;
    prompt: z.ZodString;
    references: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnion<readonly [z.ZodString, z.ZodArray<z.ZodString>]>>>;
} & { -readonly [P in keyof TExtra]: TExtra[P]; } : ({
    description: z.ZodString;
    prompt: z.ZodString;
    references: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnion<readonly [z.ZodString, z.ZodArray<z.ZodString>]>>>;
} extends infer T_1 extends z.core.util.SomeObject ? { [K in keyof T_1 as K extends keyof TExtra ? never : K]: T_1[K]; } : never) & { [K_1 in keyof { -readonly [P in keyof TExtra]: TExtra[P]; }]: { -readonly [P in keyof TExtra]: TExtra[P]; }[K_1]; }) extends infer T ? { [k in keyof T]: T[k]; } : never, z.core.$strip>;

// @public
export interface AgentPattern<I = unknown, O = unknown> extends Omit<PatternBase<I, O>, 'outputs'> {
    finish?: AgentFinishSpec<unknown, O>;
    // (undocumented)
    kind: 'agent';
    loop: {
        system: string | ((input: I, ctx: SystemPromptContext) => string);
        toolPatternIds: readonly PatternId[];
        asyncToolPatternIds?: readonly PatternId[];
        modelTags: readonly ModelTag[];
        abortMode?: 'inherit' | 'independent';
        outputExtractor?: (text: string) => unknown;
        inheritParentAssets?: boolean;
    };
    outputs?: ZodSchema<O>;
}

// @public
export type AgentPatternId = `agent_${string}`;

// @public
export interface AgentRunFacts {
    deliverables: readonly (ResolvedAssetRef & {
        label?: string;
    })[];
    stepCount: number;
    // (undocumented)
    usage?: {
        totalTokens?: number;
    };
}

// @public
export interface AgentToolDescriptor {
    description: string;
    inputSchema: unknown;
    name: string;
}

// @public
export type AgentToolRejection = {
    code: 'CIRCULAR_AGENT_TOOL';
    ancestors: readonly PatternId[];
} | {
    code: 'SUBAGENT_TOOL_OUT_OF_SCOPE';
    allowlist: readonly PatternId[];
} | {
    code: 'SUBAGENT_BLOCKED';
    matched: 'prefix' | 'id';
};

// @public
export interface Alternative<I = unknown, O = unknown> {
    appliesWhen: AlternativeAppliesWhen;
    description: string;
    id: string;
    losses?: readonly Semantics[];
    preserves?: readonly Semantics[];
    via: {
        patternId: PatternId;
        mapInput: (parentInput: I) => unknown;
        mapOutput: (childOutput: unknown) => O;
    };
}

// @public
export type AlternativeAppliesWhen =
/**
* The parent Pattern's capability (= Pattern.id) has no satisfiable
* model bearing `requiredTags`. The classic "user has no
* identity-preserving image-to-image, fall back to caption → t2i".
*/
    {
    kind: 'capability-unavailable';
    requiredTags?: readonly ModelTag[];
}
/**
* Planner / user requested one or more semantic dimensions be preserved.
* By convention a Pattern that wants this exposes a
* `requiresSemantics?: Semantics[]` field on its inputs; the resolver
* compares whatever the caller filled against `semantics`.
*/
| {
    kind: 'preserves-required';
    semantics: readonly Semantics[];
}
/**
* Always considered applicable. Useful as a last-resort fallback row
* that runs whenever earlier paths fail.
*/
| {
    kind: 'always';
};

// @public
export interface AlternativeSelectionDeps {
    // (undocumented)
    registry: PatternRegistry;
    // (undocumented)
    router: CapabilityRouter;
}

// @public
export function applicableAlternatives<I, O>(deps: AlternativeSelectionDeps, atomic: AtomicPattern<I, O>, ctx: ResolveContext, requiredTags: readonly ModelTag[], requestedSemantics: readonly Semantics[]): readonly Alternative<unknown, unknown>[];

// @public (undocumented)
export interface Artifact {
    // (undocumented)
    kind: 'image' | 'video' | 'audio' | 'text' | 'json';
    // (undocumented)
    meta?: Record<string, unknown>;
    // (undocumented)
    mime?: string;
    // (undocumented)
    uri: string;
}

// @public
export interface AskUser {
    choose<const T extends string>(input: {
        title: string;
        options: readonly T[];
    }): Promise<T>;
    confirm(input: {
        title: string;
        body?: string;
    }): Promise<boolean>;
    custom: AskUserGeneric;
    form(input: {
        title: string;
        fields: readonly AskUserFormField[];
    }): Promise<Record<string, AskUserFieldValue>>;
}

// @public (undocumented)
export type AskUserChoiceAnswer = z.infer<typeof AskUserChoiceAnswerSchema>;

// @public (undocumented)
export const AskUserChoiceAnswerSchema: z.ZodObject<{
    chosen: z.ZodString;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserChoicePayload = z.infer<typeof AskUserChoicePayloadSchema>;

// @public
export const AskUserChoicePayloadSchema: z.ZodObject<{
    title: z.ZodString;
    mode: z.ZodLiteral<"single">;
    options: z.ZodArray<z.ZodObject<{
        label: z.ZodString;
        value: z.ZodString;
    }, z.core.$strip>>;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserConfirmAnswer = z.infer<typeof AskUserConfirmAnswerSchema>;

// @public (undocumented)
export const AskUserConfirmAnswerSchema: z.ZodObject<{
    confirmed: z.ZodBoolean;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserConfirmPayload = z.infer<typeof AskUserConfirmPayloadSchema>;

// @public
export const AskUserConfirmPayloadSchema: z.ZodObject<{
    title: z.ZodString;
    body: z.ZodOptional<z.ZodString>;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserFieldValue = z.infer<typeof AskUserFieldValueSchema>;

// @public
export const AskUserFieldValueSchema: z.ZodUnion<readonly [z.ZodString, z.ZodNumber, z.ZodBoolean]>;

// @public (undocumented)
export type AskUserFormAnswer = z.infer<typeof AskUserFormAnswerSchema>;

// @public (undocumented)
export const AskUserFormAnswerSchema: z.ZodObject<{
    values: z.ZodRecord<z.ZodString, z.ZodUnion<readonly [z.ZodString, z.ZodNumber, z.ZodBoolean]>>;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserFormField = z.infer<typeof AskUserFormFieldSchema>;

// @public
export const AskUserFormFieldSchema: z.ZodObject<{
    key: z.ZodString;
    label: z.ZodString;
    type: z.ZodEnum<{
        number: "number";
        boolean: "boolean";
        text: "text";
        select: "select";
    }>;
    value: z.ZodOptional<z.ZodUnion<readonly [z.ZodString, z.ZodNumber, z.ZodBoolean]>>;
}, z.core.$strip>;

// @public (undocumented)
export type AskUserFormPayload = z.infer<typeof AskUserFormPayloadSchema>;

// @public
export const AskUserFormPayloadSchema: z.ZodObject<{
    title: z.ZodString;
    fields: z.ZodArray<z.ZodObject<{
        key: z.ZodString;
        label: z.ZodString;
        type: z.ZodEnum<{
            number: "number";
            boolean: "boolean";
            text: "text";
            select: "select";
        }>;
        value: z.ZodOptional<z.ZodUnion<readonly [z.ZodString, z.ZodNumber, z.ZodBoolean]>>;
    }, z.core.$strip>>;
}, z.core.$strip>;

// @public
export type AskUserGeneric = <TPayload = unknown, TAnswer = unknown>(opts: AskUserOptions<TPayload, TAnswer>) => Promise<TAnswer>;

// @public
export type AskUserHandler = (request: AskUserRequest) => Promise<unknown>;

// @public
export type AskUserKind = 'choice' | 'confirm' | 'form';

// @public (undocumented)
export interface AskUserOptions<TPayload = unknown, TAnswer = unknown> {
    answerSchema?: ZodSchema<TAnswer>;
    kind: AskUserKind;
    payload: TPayload;
}

// @public
export interface AskUserRequest<TPayload = unknown> {
    id: string;
    jobId?: string;
    // (undocumented)
    kind: AskUserKind;
    // (undocumented)
    payload: TPayload;
    sessionId?: string;
}

// @public
export function assertSupportedModelSpecVersion(model: {
    readonly specificationVersion?: ModelSpecVersion;
    readonly provider: string;
    readonly modelId: string;
}): void;

// @public
export const ASSET_MARKER: unique symbol;

// @public
export interface AssetAnnouncement {
    assetId: string;
    handle?: string;
    label?: string;
    // (undocumented)
    modality: AssetKind;
}

// @public
export type AssetEvent = {
    kind: 'asset';
    annotation: AssetAnnouncement;
    orderHint: number;
    batchId?: string;
};

// @public
export function assetIdField(): z.ZodString;

// @public
export interface AssetIndex {
    all(): ReadonlyArray<AssetLedgerEntry>;
    // (undocumented)
    query(spec: QuerySpec): AssetLedgerEntry[];
    resolve(handle: string): AssetLedgerEntry | undefined;
}

// @public
export type AssetKind = 'image' | 'audio' | 'video' | 'document' | 'data' | 'archive' | 'other';

// @public
export interface AssetLedgerEntry {
    // (undocumented)
    assetId: string;
    batchId?: string;
    // (undocumented)
    handle: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    sequence: number;
}

// @public
export interface AssetNeed {
    // (undocumented)
    readonly cardinality: 'single' | 'array';
    readonly description?: string;
    readonly max?: number;
    // (undocumented)
    readonly modality: AssetKind;
    // (undocumented)
    readonly required: boolean;
    readonly slot: string;
}

// @alpha
export interface AssetRecord {
    // (undocumented)
    assetId: string;
    // (undocumented)
    batchId?: string;
    // (undocumented)
    contextId: string;
    // (undocumented)
    createdAt: number;
    handle?: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    // (undocumented)
    origin: string;
    // (undocumented)
    referenceable: boolean;
    seq?: number;
}

// @public
export type AssetReferences = Record<string, string | string[]>;

// @public
export type AssetResolutionError = {
    code: 'HANDLE_NOT_FOUND';
    handle: string;
    meta: {
        available: HandleSnapshot[];
        byModality: Record<string, HandleSnapshot[]>;
    };
} | {
    code: 'MODALITY_MISMATCH';
    handle: string;
    slot: string;
    meta: {
        expected: AssetKind;
        actual: AssetKind;
        sameModalityAvailable: HandleSnapshot[];
    };
} | {
    code: 'REQUIRED_ASSET_MISSING';
    slot: string;
    modality: AssetKind;
    meta: {
        sameModalityAvailable: HandleSnapshot[];
        hint: 'upload-or-generate' | 'reference-from-history';
    };
} | {
    code: 'CARDINALITY_VIOLATION';
    slot: string;
    meta: {
        expected: 'single' | {
            min?: number;
            max?: number;
        };
        got: number;
        providedHandles: string[];
    };
} | {
    code: 'UNKNOWN_SLOT';
    slot: string;
    meta: {
        declaredSlots: string[];
        available: HandleSnapshot[];
    };
};

// @alpha
export interface AssetStore {
    // (undocumented)
    listContext(contextId: string, filter?: ListContextFilter): Promise<AssetRecord[]>;
    // (undocumented)
    record(contextId: string, ann: RecordAssetInput): Promise<AssetRecord>;
}

// @public
export interface AtomicPattern<I = unknown, O = unknown> extends PatternBase<I, O> {
    // (undocumented)
    kind: 'atomic';
    primary: PrimaryPath<I>;
}

// @public (undocumented)
export type AtomicPatternId = Capability;

// @public (undocumented)
export interface AtomicPatternInit<I = unknown, O = unknown> {
    alternatives?: readonly Alternative<I, O>[];
    assetNeeds?: readonly AssetNeed[];
    // (undocumented)
    description: string;
    exposure?: PatternExposure;
    exposureMode?: PatternExposureMode;
    // (undocumented)
    id: PatternId;
    namespace?: NamespaceId;
    // (undocumented)
    outputs: ZodSchema<O>;
    primary: PrimaryPath<I>;
    searchHint?: string;
}

// @public
export function auditOutputsSchema(schema: ZodType): OutputsSchemaAudit;

// @public
export interface AvailableAlternative {
    description: string;
    id: string;
    // (undocumented)
    losses?: readonly Semantics[];
    preserves?: readonly Semantics[];
    targetPatternId: PatternId;
}

// @public
export function boundedText(max: number): z.ZodString;

// @public
export function buildAlwaysLoadDescriptors(patterns: Iterable<Pattern>, opts?: {
    deriveProviderOptionsZod?: (id: string, baseSchema: z.ZodObject<z.ZodRawShape>) => z.ZodObject<z.ZodRawShape> | undefined;
    surface?: 'chatTurn' | 'agentLoop';
}): AgentToolDescriptor[];

// @public
export function buildAskUserFacade(ask: AskUserGeneric): AskUser;

// @public
export function buildAssetIndex(events: ReadonlyArray<AssetEvent>): AssetIndex;

// @public
export function buildCatalogDescriptors(opts?: BuildCatalogDescriptorsOptions): AgentToolDescriptor[];

// @public (undocumented)
export interface BuildCatalogDescriptorsOptions {
    includeFindPattern?: boolean;
    querySyntaxHint?: string;
    slotDefaultNote?: string;
}

// @public
export function buildFinishDescriptor(inputs: AgentFinishSpec['inputs']): AgentToolDescriptor;

// @public
export interface CallEvents {
    onArtifact?(artifact: Artifact): void;
    onProgress?(event: {
        fraction: number;
        message?: string;
    }): void;
}

// @public (undocumented)
export type Capability = 'text-generation' | 'summarization' | 'translation' | 'text-to-image' | 'image-to-image' | 'image-to-text' | 'image-segmentation' | 'text-to-video' | 'image-to-video' | 'video-to-video' | 'text-to-speech' | 'automatic-speech-recognition' | 'text-to-audio' | 'audio-to-audio' | 'embedding' | (string & {});

// @public
export interface CapabilityRouter {
    checkSatisfiable(capability: Capability, requiredTags: readonly ModelTag[], ctx: ResolveContext): SatisfiableResult;
    explain?(capability: Capability, requiredTags?: readonly ModelTag[], ctx?: ResolveContext): RoutingExplanation;
    resolve(capability: Capability, requiredTags: readonly ModelTag[], ctx: ResolveContext): ModelCapability;
}

// @public
export type CapabilitySource = 'provider-default-official' | 'provider-default-compat' | 'heuristic' | 'user' | 'probed' | 'untested';

// @public
export const consoleDiagnosticsLogger: DiagnosticsLogger;

// @public
export function createPatternFn<I = unknown, O = unknown>(patternId: PatternId): PatternFn<I, O>;

// @public
export interface CtxComputeFn {
    // (undocumented)
    <T = unknown>(id: string, fn: () => Promise<T>, options?: StepOptions): Promise<T>;
}

// @public
export interface CtxStepFn {
    // (undocumented)
    <T = unknown>(ref: PatternRef, options?: StepOptions): Promise<T>;
    // (undocumented)
    withMeta<T = unknown>(ref: PatternRef, options?: StepOptions): Promise<StepResult<T>>;
}

// @public (undocumented)
export const DEFAULT_AGENT_FINISH_SPEC: AgentFinishSpec;

// @public
export const DEFAULT_SUBAGENT_BLOCKLIST: SubagentBlocklist;

// @public (undocumented)
export function defaultAgentFinishCompose(finish: DefaultAgentFinishInput, facts: AgentRunFacts): DefaultAgentFinishOutput;

// @public (undocumented)
export type DefaultAgentFinishInput = z.infer<typeof defaultAgentFinishInputs>;

// @public (undocumented)
export const defaultAgentFinishInputs: z.ZodObject<{
    summary: z.ZodString;
    deliverables: z.ZodDefault<z.ZodArray<z.ZodObject<{
        handle: z.ZodString;
        label: z.ZodOptional<z.ZodString>;
    }, z.core.$strip>>>;
}, z.core.$strip>;

// @public (undocumented)
export type DefaultAgentFinishOutput = z.infer<typeof defaultAgentFinishOutputs>;

// @public (undocumented)
export const defaultAgentFinishOutputs: z.ZodObject<{
    assets: z.ZodArray<z.ZodObject<{
        assetId: z.ZodString;
        modality: z.ZodString;
        label: z.ZodOptional<z.ZodString>;
    }, z.core.$strip>>;
    summary: z.ZodString;
    stepCount: z.ZodNumber;
}, z.core.$strip>;

// @public
export function defineAtomicPattern<I = unknown, O = unknown>(init: AtomicPatternInit<I, O>): AtomicPattern<I, O> & {
    readonly alternatives?: readonly Alternative<I, O>[];
};

// @public
export function deriveCapabilities(inputs: readonly Modality[], outputs: readonly Modality[]): Capability[];

// @public
export type DerivedReferences<N extends readonly AssetNeed[]> = {
    [Need in N[number] as Need['slot']]?: Need['cardinality'] extends 'single' ? string : string | string[];
};

// @public
export function deriveLlmFacingInputSchema<T extends ZodRawShape>(baseSchema: z.ZodObject<T>, providerOptionsSchema: z.ZodObject<ZodRawShape> | undefined): z.ZodObject<ZodRawShape>;

// @alpha
export function deriveReferencesSchema(assetNeeds: ReadonlyArray<AssetNeed> | undefined): z.ZodOptional<z.ZodObject<z.ZodRawShape>> | undefined;

// @public
export interface DiagnosticsLogger {
    // (undocumented)
    error(message: string, detail?: unknown): void;
    // (undocumented)
    warn(message: string, detail?: unknown): void;
}

// @public
export type DispatchAudience = 'chat-turn' | 'agent-loop' | 'slash' | 'canvas';

// @public
export interface DispatchContext<P = unknown> {
    assets?: ReadonlyArray<ResolvedAssetRef>;
    project?: P;
    providerOptions?: Record<string, unknown>;
    rootJobId?: string;
    // (undocumented)
    sessionId?: string;
    signal: AbortSignal;
}

// @public (undocumented)
export type DispatchDecision = {
    kind: 'continue';
    spec?: JobSpec;
} | {
    kind: 'short-circuit';
    output: unknown;
} | {
    kind: 'reject';
    code: string;
    message: string;
};

// @public
export const dispatchEnvelopeShape: {
    readonly cost: z.ZodNullable<z.ZodNumber>;
    readonly latencyMs: z.ZodNumber;
    readonly model: z.ZodString;
    readonly provider: z.ZodString;
};

// @public
export interface DispatchMiddleware {
    afterDispatch?(job: Job, output: unknown): Promise<unknown> | unknown;
    beforeDispatch?(spec: JobSpec): Promise<DispatchDecision | undefined> | DispatchDecision | undefined;
    onError?(job: Job, error: JobError): Promise<void> | void;
}

// @public
export type DispatchPatternError = {
    code: 'PATTERN_NOT_FOUND';
    pattern_id: string;
    message: string;
    hint: string;
} | {
    code: 'AGENT_HOST_ONLY';
    pattern_id: string;
    message: string;
} | {
    code: 'INPUT_VALIDATION_FAILED';
    pattern_id: string;
    issues: readonly z.core.$ZodIssue[];
    message: string;
    hint: string;
} | {
    code: 'PATTERN_NOT_DISPATCHABLE';
    pattern_id: string;
    exposure: PatternExposure;
    audience: DispatchAudience;
    message: string;
    hint: string;
};

// @public (undocumented)
export type DispatchPatternInput = z.infer<typeof DispatchPatternInputSchema>;

// @public
export const DispatchPatternInputSchema: z.ZodObject<{
    pattern_id: z.ZodString;
    input: z.ZodRecord<z.ZodString, z.ZodUnknown>;
}, z.core.$strip>;

// @public
export interface DispatchResult<O = unknown> {
    // (undocumented)
    artifacts?: readonly Artifact[];
    // (undocumented)
    output: O;
    terminate?: boolean;
}

// @public
export interface ExecutionContext<S = unknown, P = unknown, W = unknown> extends DispatchContext<P> {
    askUser: AskUser;
    compute: CtxComputeFn;
    // (undocumented)
    session?: S;
    step: CtxStepFn;
    stepIndex: number;
    submitJob<TIn = unknown, TOut = unknown>(spec: JobSpec<TIn>): Promise<Job<TIn, TOut>>;
    // (undocumented)
    workflow?: W;
}

// @public
export function extendInputsWithReferences<I>(patternId: string, inputs: ZodSchema<I>, assetNeeds: ReadonlyArray<AssetNeed> | undefined): ZodSchema<I>;

// @public (undocumented)
export type FindPatternInput = z.infer<typeof FindPatternInputSchema>;

// @public
export const FindPatternInputSchema: z.ZodObject<{
    query: z.ZodString;
    kind: z.ZodOptional<z.ZodEnum<{
        agent: "agent";
        atomic: "atomic";
        meta: "meta";
    }>>;
    modality: z.ZodOptional<z.ZodEnum<{
        image: "image";
        audio: "audio";
        video: "video";
        text: "text";
    }>>;
}, z.core.$strip>;

// @public
export function formatRoutingExplanation(explanation: RoutingExplanation): string;

// @public
export function fromAssetUri(ref: string): string;

// @public
export type Handle = string;

// @public
export interface HandleSnapshot {
    // (undocumented)
    batchId?: string;
    // (undocumented)
    handle: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    // (undocumented)
    sequence: number;
}

// @public
export function inferNamespace(id: PatternId): NamespaceId;

// @public (undocumented)
export function isAssetUri(ref: string): boolean;

// @public
export function isDispatchError(result: ResolvedDispatchTarget | DispatchPatternError): result is DispatchPatternError;

// @public
export interface Job<TInput = unknown, TOutput = unknown> {
    // (undocumented)
    createdAt: number;
    // (undocumented)
    error: JobError | null;
    // (undocumented)
    id: string;
    idempotencyKey: string;
    // (undocumented)
    input: TInput;
    jobKind?: JobKind;
    // (undocumented)
    output: TOutput | null;
    // (undocumented)
    patternId: PatternId;
    // (undocumented)
    sessionId?: string;
    // (undocumented)
    status: JobStatus;
    // (undocumented)
    updatedAt: number;
}

// @public (undocumented)
export interface JobError {
    cause?: unknown;
    // (undocumented)
    code: string;
    // (undocumented)
    details?: unknown;
    // (undocumented)
    message: string;
    producedAssets?: readonly string[];
}

// @public
export type JobEvent<TInput = unknown, TOutput = unknown> = {
    type: 'job:submitted';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:started';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:progress';
    job: Job<TInput, TOutput>;
    fraction: number;
    message?: string;
} | {
    type: 'job:artifact';
    job: Job<TInput, TOutput>;
    artifact: Artifact;
} | {
    type: 'job:step';
    job: Job<TInput, TOutput>;
    stepId: string;
    patternId: PatternId;
    childJobId: string;
    assets?: readonly {
        assetId: string;
        modality: string;
        url?: string;
    }[];
} | {
    type: 'job:output';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:model-fallback';
    job: Job<TInput, TOutput>;
    capability: Capability;
    failedModel: string;
    hop: number;
    attempts: number;
    error: JobError;
} | {
    type: 'job:alternative-selected';
    job: Job<TInput, TOutput>;
    alternativeId: string;
    description: string;
    targetPatternId: PatternId;
    preserves?: readonly Semantics[];
    losses?: readonly Semantics[];
} | ({
    type: 'job:tool-rejected';
    job: Job<TInput, TOutput>;
    patternId: PatternId;
    callerPatternId: PatternId;
    via?: PatternId;
} & AgentToolRejection) | {
    type: 'job:completed';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:failed';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:cancelled';
    job: Job<TInput, TOutput>;
} | {
    type: 'job:stale';
    job: Job<TInput, TOutput>;
};

// @public
export type JobKind = 'pattern' | 'agent';

// @public (undocumented)
export interface JobQueryFilter {
    // (undocumented)
    patternId?: string;
    // (undocumented)
    sessionId?: string;
    since?: number;
    // (undocumented)
    status?: readonly JobStatus[];
}

// @public
export interface JobSpec<TInput = unknown> {
    assetContextId?: string;
    assets?: readonly ResolvedAssetRef[];
    idempotencyKey?: string;
    // (undocumented)
    input: TInput;
    jobKind?: JobKind;
    // (undocumented)
    patternId: PatternId;
    providerOptions?: Record<string, unknown>;
    resolveHints?: {
        excludeModel?: readonly string[];
    };
    resumeFromRunId?: string;
    // (undocumented)
    sessionId?: string;
    stepIdNamespace?: string;
    stepIndex?: number;
    stepKey?: string;
}

// @public (undocumented)
export type JobStatus = 'queued' | 'running' | 'done' | 'error' | 'cancelled'
/**
* Runtime found this row in 'running' at startup with no live controller
* and abandoned it — no way to tell whether the provider call ever landed.
*/
| 'stale';

// @public (undocumented)
export interface JobStore {
    conditionalUpdate(id: string, patch: Partial<Job>, ifStatus: JobStatus): Promise<boolean>;
    findByIdempotencyKey(key: string): Promise<Job | null>;
    // (undocumented)
    get(id: string): Promise<Job | null>;
    insert(job: Job): Promise<void>;
    insertIfAbsent(job: Job): Promise<Job>;
    // (undocumented)
    query(filter?: JobQueryFilter): Promise<readonly Job[]>;
    subscribe?(callback: (event: JobEvent) => void): Unsubscribe;
    // (undocumented)
    update(id: string, patch: Partial<Job>): Promise<void>;
}

// @public
export interface JsonSchema {
    readonly [k: string]: unknown;
    // (undocumented)
    readonly additionalProperties?: boolean | JsonSchema;
    // (undocumented)
    readonly allOf?: readonly JsonSchema[];
    // (undocumented)
    readonly anyOf?: readonly JsonSchema[];
    // (undocumented)
    readonly const?: unknown;
    // (undocumented)
    readonly default?: unknown;
    // (undocumented)
    readonly description?: string;
    // (undocumented)
    readonly enum?: readonly unknown[];
    // (undocumented)
    readonly exclusiveMaximum?: number;
    // (undocumented)
    readonly exclusiveMinimum?: number;
    // (undocumented)
    readonly items?: JsonSchema | readonly JsonSchema[];
    // (undocumented)
    readonly maximum?: number;
    // (undocumented)
    readonly maxItems?: number;
    // (undocumented)
    readonly maxLength?: number;
    // (undocumented)
    readonly minimum?: number;
    // (undocumented)
    readonly minItems?: number;
    // (undocumented)
    readonly minLength?: number;
    // (undocumented)
    readonly oneOf?: readonly JsonSchema[];
    // (undocumented)
    readonly properties?: Readonly<Record<string, JsonSchema>>;
    // (undocumented)
    readonly required?: readonly string[];
    // (undocumented)
    readonly type?: string | readonly string[];
}

// @public
export const LIFT_MARKER: unique symbol;

// @alpha
export interface ListContextFilter {
    includeNonReferenceable?: boolean;
    origins?: readonly string[];
}

// @public
export class ManifestError extends Error {
    constructor(code: ManifestErrorCode, detail: string);
    // (undocumented)
    readonly code: ManifestErrorCode;
}

// @public (undocumented)
export type ManifestErrorCode =
/** The `"orchestral"` field does not match {@link OrchestralManifestSchema}. */
'MANIFEST_INVALID'
/** `only` named an id the manifest does not declare — typo, or a moved pattern. */
| 'MANIFEST_UNKNOWN_PATTERN'
/**
* A pattern's `requiredOps` entry is absent from the ops object the caller
* passed, under the default `missingOps: 'throw'`.
*/
| 'MANIFEST_MISSING_OPS'
/** No export by that name on the module — usually a renamed factory. */
| 'MANIFEST_EXPORT_MISSING'
/** The export exists but is not callable. */
| 'MANIFEST_EXPORT_NOT_A_FACTORY'
/** The factory ran but produced a different id / kind than declared. */
| 'MANIFEST_PATTERN_MISMATCH';

// @public
export function matchSubagentBlocklist(id: PatternId, blocklist?: SubagentBlocklist): 'prefix' | 'id' | null;

// @public
export const metaEnvelopeShape: {
    readonly cost: z.ZodNullable<z.ZodNumber>;
    readonly latencyMs: z.ZodNumber;
};

// @public
export interface MetaPattern<I = unknown, O = unknown> extends PatternBase<I, O> {
    compose(params: {
        input: I;
    }, ctx: ExecutionContext): Promise<O>;
    // (undocumented)
    kind: 'meta';
    plannedDispatches?: (input: I) => readonly PatternId[];
    tool: ToolDescriptor<I>;
}

// @public (undocumented)
export type MetaPatternId = `meta_${string}`;

// @public
export function mintHandle(modality: AssetKind, priorCountOfModality: number): string;

// @public
export type Modality = 'text' | 'image' | 'audio' | 'video' | 'embedding';

// @public
export const MODEL_SPEC_VERSION: ModelSpecVersion;

// @public
export interface ModelCapability extends ModelCapabilityRecord {
    // (undocumented)
    call<I = unknown, O = unknown>(input: I, ctx: DispatchContext, events?: CallEvents): Promise<DispatchResult<O>>;
    readonly specificationVersion?: ModelSpecVersion;
}

// @public
export interface ModelCapabilityBlob {
    capabilities: readonly Capability[];
    // (undocumented)
    contextWindow?: number;
    // (undocumented)
    deprecated?: boolean;
    inputs: readonly Modality[];
    // (undocumented)
    outputs: readonly Modality[];
    providerOptions?: JsonSchema;
    providerOptionsMode?: 'official' | 'custom';
    source: CapabilitySource;
    streaming?: boolean;
    // (undocumented)
    structuredOutput?: boolean;
    tags?: readonly ModelTag[];
    tier?: 'fast' | 'balanced' | 'premium';
    // (undocumented)
    toolUse?: boolean;
}

// @public
export interface ModelCapabilityRecord extends ModelCapabilityBlob {
    modelId: string;
    // (undocumented)
    provider: string;
    tags: readonly ModelTag[];
}

// @public
export interface ModelFacingAsset {
    // (undocumented)
    handle: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    uri: string;
}

// @public
export interface ModelFacingOutputAsset {
    from?: {
        handle: string;
        role: string;
    }[];
    // (undocumented)
    handle: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    origin?: 'generated';
    uri: string;
}

// @public
export type ModelSpecVersion = 'v1';

// @public
export class ModelSpecVersionUnsupportedError extends Error {
    constructor(model: string, received: string);
    // (undocumented)
    readonly code = "MODEL_SPEC_VERSION_UNSUPPORTED";
    // (undocumented)
    readonly diagnostic: {
        model: string;
        received: string;
        supported: readonly ModelSpecVersion[];
        hint: string;
    };
}

// @public
export type ModelTag =
/** Sub-second / step-distilled inference (distilled / few-step models). */
'fast-distilled'
/** Generic latency tiers (also expressed on ModelCapability.tier). */
| 'fast' | 'balanced' | 'premium' | 'streaming' | 'tool-use' | 'structured-output' | (string & {});

// @public
export type NamespaceId = 'image-gen' | 'video-gen' | 'audio-gen' | 'text-gen' | 'meta-pipelines' | 'sub-agents' | 'uncategorized' | (string & {});

// @public
export type OnStrip = (info: {
    path: string;
    reason: StripReason;
}) => void;

// @public
export function opaqueToken(max?: number): z.ZodString;

// @public (undocumented)
export type OrchestralManifest = z.infer<typeof OrchestralManifestSchema>;

// @public (undocumented)
export type OrchestralManifestPattern = z.infer<typeof OrchestralManifestPatternSchema>;

// @public
export const OrchestralManifestPatternSchema: z.ZodObject<{
    id: z.ZodString;
    kind: z.ZodEnum<{
        agent: "agent";
        atomic: "atomic";
        meta: "meta";
    }>;
    export: z.ZodString;
    requiredOps: z.ZodOptional<z.ZodArray<z.ZodString>>;
}, z.core.$strip>;

// @public
export const OrchestralManifestSchema: z.ZodObject<{
    patterns: z.ZodArray<z.ZodObject<{
        id: z.ZodString;
        kind: z.ZodEnum<{
            agent: "agent";
            atomic: "atomic";
            meta: "meta";
        }>;
        export: z.ZodString;
        requiredOps: z.ZodOptional<z.ZodArray<z.ZodString>>;
    }, z.core.$strip>>;
    requiredOps: z.ZodOptional<z.ZodUndefined>;
}, z.core.$strip>;

// @public (undocumented)
export interface OutputsSchemaAudit {
    notTraversed: string[];
    unbounded: string[];
}

// @public (undocumented)
export const parallel: ParallelFn;

// @public (undocumented)
export interface ParallelFn {
    // (undocumented)
    <T extends readonly unknown[] | []>(promises: T): Promise<{
        -readonly [K in keyof T]: Awaited<T[K]>;
    }>;
    // (undocumented)
    allSettled<T extends readonly unknown[] | []>(promises: T): Promise<{
        -readonly [K in keyof T]: PromiseSettledResult<Awaited<T[K]>>;
    }>;
}

// @public
export type Pattern<I = unknown, O = unknown> = AtomicPattern<I, O> | MetaPattern<I, O> | AgentPattern<I, O>;

// @public
export interface PatternBase<I = unknown, O = unknown> {
    assetNeeds?: readonly AssetNeed[];
    checkPermissions?: (input: I, ctx: DispatchContext) => Promise<PermissionResult> | PermissionResult;
    defaultExecutionMode?: 'sync' | 'async';
    description: string;
    exposure?: PatternExposure;
    exposureMode?: PatternExposureMode;
    extensible?: boolean;
    // (undocumented)
    id: PatternId;
    namespace?: NamespaceId;
    origin?: 'plan';
    outputs: ZodSchema<O>;
    primary?: PrimaryPath<I>;
    searchHint?: string;
}

// @public
export type PatternExposure = PatternExposureShorthand | PatternExposureMap;

// @public
export interface PatternExposureMap {
    // (undocumented)
    agentLoop?: boolean;
    // (undocumented)
    canvas?: boolean;
    // (undocumented)
    chatTurn?: boolean;
    // (undocumented)
    host?: boolean;
    // (undocumented)
    slash?: boolean;
}

// @public
export type PatternExposureMode = 'always-load' | 'deferred';

// @public
export type PatternExposureShorthand = 'tool' | 'agent-tool' | 'no-tool';

// @public
export interface PatternFn<I, O> {
    // (undocumented)
    (ctx: ExecutionContext, input: I, options?: PatternFnOptions): Promise<O>;
    readonly patternId: PatternId;
    // (undocumented)
    withMeta(ctx: ExecutionContext, input: I, options?: PatternFnOptions): Promise<StepResult<O>>;
}

// @public
export type PatternFnOptions = StepOptions & {
    assets?: PatternRef['assets'];
};

// @public (undocumented)
export type PatternId = AtomicPatternId | MetaPatternId | AgentPatternId;

// @public
export interface PatternRef {
    assets?: readonly ResolvedAssetRef[];
    // (undocumented)
    input: unknown;
    // (undocumented)
    patternId: PatternId;
}

// @public
export class PatternRegistry {
    // (undocumented)
    [Symbol.iterator](): IterableIterator<Pattern>;
    constructor(options?: {
        logger?: DiagnosticsLogger;
    });
    add<I = unknown, O = unknown>(spec: Pattern<I, O> & {
        alternatives?: readonly Alternative<I, O>[];
    }): void;
    addFromManifest(manifest: unknown, module: Readonly<Record<string, unknown>>, ops?: Readonly<Record<string, unknown>>, options?: AddFromManifestOptions): AddFromManifestResult;
    attachAlternative<I, O>(parentId: PatternId, alt: Alternative<I, O>): Unsubscribe;
    byNamespace(ns: NamespaceId): readonly Pattern[];
    // (undocumented)
    get(id: PatternId): Pattern | undefined;
    getEntry(id: PatternId): RegistryEntry | undefined;
    // (undocumented)
    has(id: PatternId): boolean;
    listForCatalog(excludeIds?: readonly PatternId[]): Array<{
        patternId: PatternId;
        shortName: string;
        description: string;
        primaryDescription: string;
        primaryInputs: ZodSchema<unknown>;
    }>;
    register<I = unknown, O = unknown>(pattern: Pattern<I, O> & {
        alternatives?: readonly Alternative<I, O>[];
    }): void;
    resolveShortName(short: string): PatternId | undefined;
    scope(): PatternScope;
    // (undocumented)
    size(): number;
    // (undocumented)
    unregister(id: PatternId): boolean;
    // (undocumented)
    values(): IterableIterator<Pattern>;
}

// @public
export interface PatternScope {
    add<I = unknown, O = unknown>(spec: Pattern<I, O> & {
        alternatives?: readonly Alternative<I, O>[];
    }): void;
    dispose(): void;
}

// @public
export type PatternSearch = (req: PatternSearchRequest) => unknown;

// @public
export interface PatternSearchRequest {
    audience: 'chat-turn' | 'agent-loop';
    directToolIds?: ReadonlySet<string>;
    excludeIds?: ReadonlySet<string>;
    includeOnly?: ReadonlySet<string>;
    input: FindPatternInput;
    resolveCtx?: ResolveContext;
}

// @public
export type PermissionResult = {
    ok: true;
} | {
    ok: false;
    reason: string;
    remedy?: 'reauthorize' | 'reduce-scope' | 'manual';
};

// @public
export function pickAlternative<I, O>(deps: AlternativeSelectionDeps, atomic: AtomicPattern<I, O>, ctx: ResolveContext, requiredTags: readonly ModelTag[], requestedSemantics: readonly Semantics[]): Alternative<I, O> | null;

// @public
export interface PrimaryPath<I = unknown> {
    modelTags?: readonly ModelTag[];
    tool: ToolDescriptor<I>;
}

// @public
export type ProducedAssetModality = 'image' | 'audio' | 'video';

// @public
export function producedAssetShape<M extends ProducedAssetModality>(modality: M): {
    readonly assetId: z.ZodString;
    readonly modality: z.ZodLiteral<M>;
    readonly url: z.ZodOptional<z.ZodString>;
    readonly cost: z.ZodOptional<z.ZodNumber>;
};

// @public
export function projectAssetsForModel(assets: ReadonlyArray<AssetLedgerEntry>): ReadonlyArray<ModelFacingAsset>;

// @public
export function projectToolOutputForModel(output: unknown): unknown;

// @public
export interface QuerySpec {
    // (undocumented)
    modality: AssetKind;
    // (undocumented)
    mode: 'latestOfModality' | 'latestBatchOfModality';
}

// @public
export function readRequiresSemantics(input: unknown): readonly Semantics[];

// @alpha
export interface RecordAssetInput {
    // (undocumented)
    assetId: string;
    batchId?: string;
    handle?: string;
    // (undocumented)
    label?: string;
    // (undocumented)
    modality: AssetKind;
    origin: string;
    owner?: string;
    referenceable?: boolean;
}

// @public
export interface RegistryEntry {
    // (undocumented)
    alternatives: readonly Alternative<unknown, unknown>[];
    // (undocumented)
    pattern: Pattern;
}

// @public
export function resolveAssetReferences(inputs: {
    references?: AssetReferences;
} | null | undefined, assetNeeds: ReadonlyArray<AssetNeed>, index: AssetIndex): {
    ok: true;
    assets: ReadonlyArray<ResolvedAssetRef>;
} | {
    ok: false;
    error: AssetResolutionError;
};

// @public
export interface ResolveContext {
    excludeModel?: readonly string[];
    // (undocumented)
    excludeProvider?: readonly string[];
    fallbackDepth?: number;
    pinnedModel?: string;
    // (undocumented)
    preferProvider?: string;
    rankedModels?: readonly string[];
    // (undocumented)
    tier?: 'fast' | 'balanced' | 'premium';
}

// @public
export type ResolveCtxProvider = (spec: JobSpec) => ResolveContext;

// @public
export interface ResolvedAssetRef {
    // (undocumented)
    assetId: string;
    handle?: string;
    // (undocumented)
    modality: AssetKind;
    // (undocumented)
    slot: string;
}

// @public (undocumented)
export interface ResolvedDispatchTarget {
    parsedInput: Record<string, unknown>;
    // (undocumented)
    pattern: Pattern;
}

// @alpha
export interface ResolvedExposure {
    // (undocumented)
    agentLoop: boolean;
    // (undocumented)
    canvas: boolean;
    // (undocumented)
    chatTurn: boolean;
    // (undocumented)
    host: boolean;
    // (undocumented)
    slash: boolean;
}

// @public
export function resolveDispatchTarget(registry: PatternRegistry, input: DispatchPatternInput, audience: DispatchAudience): ResolvedDispatchTarget | DispatchPatternError;

// @alpha
export function resolveExposure(e?: PatternExposure): ResolvedExposure;

// @public
export function resolveNamespace(id: PatternId, declared: NamespaceId | undefined): NamespaceId;

// @alpha
export function resolveSlashDispatch(registry: PatternRegistry, patternId: string): SlashDispatchResolution;

// @public
export type RetryPolicy = {
    kind: 'none';
} | {
    kind: 'exponential';
    maxAttempts: number;
    baseMs: number;
    maxMs?: number;
} | {
    kind: 'fixed';
    maxAttempts: number;
    delayMs: number;
};

// Warning: (ae-forgotten-export) The symbol "RoutingCandidateBase" needs to be exported by the entry point index.d.ts
//
// @public
export type RoutingCandidate = (RoutingCandidateBase & {
    kept: true;
}) | (RoutingCandidateBase & {
    kept: false;
    droppedBy: RoutingDropStage;
});

// @public
export type RoutingDropStage = 'capability-not-declared' | 'not-enabled' | 'not-ranked' | 'excluded-provider' | 'excluded-model' | 'tag-mismatch';

// @public
export interface RoutingExplanation {
    candidates: readonly RoutingCandidate[];
    // (undocumented)
    capability: Capability;
    context: ResolveContext;
    enablementDefaulted: boolean;
    order: readonly string[];
    // (undocumented)
    outcome: RoutingOutcome;
    // (undocumented)
    requiredTags: readonly ModelTag[];
    satisfiable: boolean;
}

// @public
export type RoutingOutcome = {
    kind: 'selected';
    model: string;
    by: RoutingSelectionRule;
} | {
    kind: 'no-candidate';
    reason: UnavailabilityReason;
} | {
    kind: 'pin-excluded';
    pinnedModel: string;
    excludedByRetry: boolean;
};

// @public
export type RoutingSelectionRule = 'pinned' | 'preferred-provider' | 'tier' | 'first-candidate';

// @public (undocumented)
export interface Runtime {
    abandonOrphanedJobs(): Promise<readonly Job[]>;
    // (undocumented)
    cancelJob(jobId: string, reason?: string): Promise<void>;
    // @alpha
    getAgentEnvelope?(jobId: string): AgentDispatchEnvelope | undefined;
    // (undocumented)
    pollJob<TIn = unknown, TOut = unknown>(jobId: string): Promise<Job<TIn, TOut>>;
    submitJob<TIn = unknown, TOut = unknown>(spec: JobSpec<TIn>): Promise<Job<TIn, TOut>>;
    // (undocumented)
    subscribe(jobId: string, cb: (ev: JobEvent) => void): Unsubscribe;
}

// @public (undocumented)
export function sanitizeToolOutput<T>(output: T, options?: SanitizeToolOutputOptions): T;

// @public (undocumented)
export interface SanitizeToolOutputOptions {
    base64RunMin?: number;
    controlRatio?: number;
    maxInlineLen?: number;
    onStrip?: OnStrip;
}

// @public
export type SatisfiableResult = {
    ok: true;
    candidates: readonly ModelCapabilityRecord[];
} | {
    ok: false;
    reason: UnavailabilityReason;
    candidates: readonly ModelCapabilityRecord[];
};

// @public
export type Semantics =
/** Subject preservation (IP-Adapter / InstantID / PuLID territory) */
'subject-identity'
/** Visual style preservation across edits */
| 'style'
/** Composition / framing preservation */
| 'composition'
/** Same character across multiple shots / a longer sequence */
| 'character-consistency'
/** TTS / dubbing alignment with mouth movements */
| 'lipsync-accuracy'
/** Smooth motion / continuity across video frames */
| 'temporal-coherence' | (string & {});

// @alpha
export function setAssetUriScheme(next: string): void;

// @public
export const silentDiagnosticsLogger: DiagnosticsLogger;

// @public
export interface SkippedManifestPattern {
    // (undocumented)
    readonly id: PatternId;
    readonly missingOps: readonly string[];
}

// @alpha
export type SlashDispatchError = {
    code: 'SLASH_PATTERN_NOT_FOUND';
    patternId: string;
    message: string;
} | {
    code: 'SLASH_NOT_EXPOSED';
    patternId: string;
    message: string;
};

// @alpha
export type SlashDispatchResolution = {
    ok: true;
    fullId: string;
} | {
    ok: false;
    error: SlashDispatchError;
};

// @public (undocumented)
export interface StepMeta {
    // (undocumented)
    attempts: number;
    // (undocumented)
    durationMs: number;
    // (undocumented)
    stepId: string;
}

// @public (undocumented)
export interface StepOptions {
    identity?: 'index' | 'id';
    metadata?: Record<string, unknown>;
    retry?: RetryPolicy;
    stepId?: string;
}

// @public (undocumented)
export interface StepResult<T> {
    // (undocumented)
    meta: StepMeta;
    // (undocumented)
    value: T;
}

// @alpha
export type StopConditionDescriptor = {
    kind: 'step-count';
    n: number;
} | {
    kind: 'token-count';
    n: number;
};

// @public (undocumented)
export type StripReason = 'data-url' | 'binary-run' | 'control-chars';

// @public
export interface SubagentBlocklist {
    // (undocumented)
    readonly idPrefixes: readonly string[];
    // (undocumented)
    readonly patternIds: readonly PatternId[];
}

// @public
export function sumCosts(costs: readonly (number | null | undefined)[]): number | null;

// @public
export const SUPPORTED_MODEL_SPEC_VERSIONS: readonly ModelSpecVersion[];

// @public
export type SystemPromptContext<P = unknown> = Omit<DispatchContext<P>, 'assets'>;

// @public (undocumented)
export function toAssetUri(handle: string): string;

// @public
export function toAvailableAlternative(alt: Alternative<unknown, unknown>): AvailableAlternative;

// @public
export function toJsonSchema<T>(zodSchema: ZodType<T>): JsonSchema;

// @public
export interface ToolDescriptor<I = unknown> {
    description: string;
    inputs: ZodSchema<I>;
    isConcurrencySafe?: (input: I) => boolean;
    isDestructive?: (input: I) => boolean;
    longDescription?: string;
}

// @public
export interface TranscriptMessage {
    kind: 'assistant' | 'tool-result' | 'progress' | 'compact-boundary';
    raw: unknown;
    seq: number;
    ts: number;
}

// @public
export interface TranscriptMessageWithAgent extends TranscriptMessage {
    // (undocumented)
    agentId: string;
}

// @public (undocumented)
export interface TranscriptStore {
    append(runId: string, agentId: string, msg: TranscriptMessage): Promise<void>;
    clear(runId: string): Promise<void>;
    read(runId: string, agentId: string): Promise<readonly TranscriptMessage[]>;
    readAll(runId: string, filterPatternIdPrefix?: string): Promise<readonly TranscriptMessageWithAgent[]>;
}

// @public
export type UnavailabilityReason =
/** No model in the user's catalog declares the requested capability. */
'no-model-in-catalog'
/**
* Declared-but-not-enabled: models in the catalog declare the capability,
* but the host's enablement list excludes every capable model (the user
* never enabled one for this capability).
*/
| 'not-enabled'
/** Every candidate has been excluded via ResolveContext.excludeModel. */
| 'all-excluded'
/** Requested tier (fast / balanced / premium) has no candidates. */
| 'tier-mismatch'
/** No model bears every requested ModelTag. */
| 'tag-mismatch';

// @public (undocumented)
export type Unsubscribe = () => void;

// @public
export function urlField(max?: number): z.ZodString;

// @public
export const whenAlways: AlternativeAppliesWhen;

// @public
export const whenCapabilityUnavailable: (...requiredTags: readonly ModelTag[]) => AlternativeAppliesWhen;

// @public
export const whenPreservesRequired: (...semantics: readonly Semantics[]) => AlternativeAppliesWhen;

// @public (undocumented)
export type ZodSchema<T = unknown> = z.ZodType<T>;

// (No @packageDocumentation comment for this package)

```
