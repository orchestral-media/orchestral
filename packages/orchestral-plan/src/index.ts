// @orchestral/plan — a pipeline authored as data: the schema, the walk, the
// interpreter and the preflight, in one package.
//
// Deliberately empty at this commit: the scaffold lands first so the migration
// that follows is a move between two existing packages rather than a move and a
// build system at once.

export {}
