{
  "name": "incremental-rerun",
  "private": true,
  "type": "module",
  "description": "Minimal host: re-submit a three-step meta with one input changed and watch the unchanged steps come back from the JobStore under their original child job ids — content-addressed dedup as a node cache, mock models only.",
  "scripts": {
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@orchestral/core": "workspace:*",
    "@orchestral/runtime": "workspace:*",
    "@orchestral/patterns": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  }
}
